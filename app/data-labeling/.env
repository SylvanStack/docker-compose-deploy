# ==================== 基础配置 ====================
# 数据存储根路径
# 服务器
#ROOT_PATH=/opt/docker_volume
# WINDOWS SERVER
ROOT_PATH=D:\volume
# 本地
# ROOT_PATH=/Users/sylvan/docker_volume

# Label Studio 访问地址（修改为你的服务器IP或域名）
LABEL_STUDIO_HOST=http://localhost:11000

# HTTP 端口
HTTP_PORT=11000
# HTTPS 端口（如需要）
# HTTPS_PORT=11443

# ==================== 数据库配置 ====================
DJANGO_DB=default
POSTGRES_DB=labelstudio
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=label-studio-db
POSTGRES_PORT=5432
POSTGRES_EXTERNAL_PORT=15432

# PostgreSQL SSL 配置（可选）
# POSTGRE_SSL_MODE=require
# POSTGRE_SSLROOTCERT=/certs/ca-cert.pem

# ==================== Nginx 配置 ====================
APP_HOST=label-studio-app
USE_NGINX_FOR_UPLOADS=true
USE_NGINX_FOR_EXPORT_DOWNLOADS=true

# SSL/TLS 配置（可选）
# NGINX_SSL_CERT=/certs/cert.pem
# NGINX_SSL_CERT_KEY=/certs/cert-key.pem

# Nginx 日志级别: debug, info, notice, warn, error, crit, alert, emerg
NGINX_ERROR_LOG_LEVEL=warn

# ==================== 应用日志配置 ====================
LOG_LEVEL=INFO
# LOG_DIR=/label-studio/data/logs

# ==================== 数据目录配置 ====================
# 注意：这是容器内的路径，对应 docker-compose 中挂载的 /label-studio/data
LABEL_STUDIO_BASE_DATA_DIR=/label-studio/data
LABEL_STUDIO_LOCAL_FILES_DOCUMENT_ROOT=/label-studio/data/media

# ==================== 用户认证配置 ====================
# 禁用公开注册（仅管理员可创建用户）
# DISABLE_SIGNUP_WITHOUT_LINK=true

# LDAP 认证配置（可选）
# LDAP_ENABLED=true
# LDAP_SERVER_URI=ldap://ldap.example.com
# LDAP_BIND_DN=cn=admin,dc=example,dc=com
# LDAP_BIND_PASSWORD=password
# LDAP_USER_SEARCH_BASE=ou=users,dc=example,dc=com

# ==================== 会话和安全配置 ====================
# Session 超时时间（秒）
# SESSION_COOKIE_AGE=86400
# CSRF 配置
# CSRF_TRUSTED_ORIGINS=http://localhost:11000
# 允许的主机
# ALLOWED_HOSTS=*

# ==================== 存储配置 ====================
# AWS S3 配置（可选）
# USE_DEFAULT_S3=true
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_STORAGE_BUCKET_NAME=your-bucket
# AWS_S3_REGION_NAME=us-east-1
# AWS_S3_ENDPOINT_URL=https://s3.amazonaws.com

# MinIO 配置（本地 S3 兼容存储，可选）
 MINIO_STORAGE_ENDPOINT=http://minio:9000
 MINIO_STORAGE_ACCESS_KEY=minioadmin
 MINIO_STORAGE_SECRET_KEY=minioadmin
 MINIO_STORAGE_BUCKET_NAME=label-studio

# Google Cloud Storage 配置（可选）
# GOOGLE_APPLICATION_CREDENTIALS=/opt/heartex/secrets/key.json
# GCS_BUCKET_NAME=your-bucket

# Azure Blob Storage 配置（可选）
# AZURE_STORAGE_ACCOUNT_NAME=mystorageaccount
# AZURE_STORAGE_ACCOUNT_KEY=your-account-key
# AZURE_CONTAINER_NAME=label-studio

# ==================== Redis 配置（可选）====================
# REDIS_LOCATION=redis://redis:6379/1
# REDIS_PASSWORD=your-redis-password

# ==================== 邮件配置（可选）====================
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=true
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-password
# DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# ==================== 性能配置 ====================
# 最大上传文件大小（MB）
# MAX_UPLOAD_SIZE=100
# 数据库连接池大小
# DB_CONN_MAX_AGE=600
# uWSGI 进程和线程数
# UWSGI_PROCESSES=4
# UWSGI_THREADS=2

# ==================== 功能开关 ====================
# 启用机器学习后端
# ML_BACKEND_ENABLED=true
# 启用 Webhooks
# WEBHOOK_ENABLED=true
# 启用数据导入
# ENABLE_IMPORT=true

# ==================== 调试模式（生产环境请设为 false）====================
# DEBUG=false